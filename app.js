// app.js

// Import the Circle module
const circle = require('./circle');

// Example usage
const radius = 5;

console.log(`Area of the circle with radius ${radius} is: ${circle.calculateArea(radius)}`);
console.log(`Perimeter (Circumference) of the circle with radius ${radius} is: ${circle.calculatePerimeter(radius)}`);
